{
  "name": "refund",
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "setup": "npm install && npx papi add polkadot -n polkadot && npx papi add kusama -n ksmcc3 && npx papi add collectives -n polkadot_collectives && npx papi",
    "start": "node index.js"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@polkadot-api/descriptors": "file:.papi/descriptors",
    "dialog": "^0.3.1",
    "express": "^4.19.2",
    "node-notifier": "^10.0.1",
    "polkadot-api": "^1.1.0"
  }
}
